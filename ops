{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":3,"timestamp":1789789627,"nonce":"IVCKZXkY/06bo9B2Tt7lffU+pgI=","message":"Step 3 landed in 243a27de395292025aaf82f91601f72173c4d188: Ty::Dict(shape) with the representation unchanged and typed reads, so BODIES is Dict(Str -\u003e Tuple(8)), SYSTEM List(Tuple(8)), combinations returns List(Tuple(pair)) (returning a local list counts as a read; the return type is decided afresh per round; two lists of tuple shapes join when one refines the other) and advance's loop has 26 calls, 21 of them cold zb_fatal. nbody_modified: 7.1 to 7.3 ms per iteration against PyPy 7.3 to 7.5 warm on the same box; the LLVM tier now promotes advance and runs it at the same speed as Cranelift. Step 4 (per-shape function instances) remains. A list inside a tuple is stored as its box; storing the header's address (Ptr to the header, no box) would save an indirection and an allocation per tuple and is the next refinement.","files":null}]}