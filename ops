{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1789483859,"nonce":"HkBouTk6HvT++PlK/KKutIkuV4Y=","title":"String concatenation builds a Rust String before the result","message":"`$IO$string_concat`, which the compiler lowers every string `+` to, pushed both operands into a Rust `String` (a system allocation, UTF-8 validation of both, growth) and then copied that into a fresh string from the program's heap. Measured on the M1 Pro at about 56 ns for a six-byte result; `str(i)` with its one allocation took 12 ns beside it.\n\nNow one allocation of the final length and two copies, as the string plugin's own concat does. 15 ns for the same result; bench_tokenize's string-building loop (300 k of `words[i % 8] + str(i % 100)`) 21 to 8 ms.","files":null}]}