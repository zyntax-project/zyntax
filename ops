{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1789485424,"nonce":"sEJ7ZS+hDt0WLt0sRtnpiuBap80=","title":"Arithmetic between a dynamic value and an integer boxes the integer","message":"`d.get(k, 0) + 1`, with the left side dynamic and the right a known int, boxed the 1 (a shared box now, still a call and a release afterwards), then `zb_any_arith` read both tags. Now the frontend calls `zb_any_arith_i64` (or `zb_i64_arith_any` with the integer on the left), which computes directly when the dynamic side is a number and boxes the integer only for the general case (instances, strings under `*`).\n\nMeasured on the M1 Pro, exec ms: the tokenize counting loop's `v = counts.get(tok, 0) + 1` stage 49 to 42.5 on 300 k iterations; bench_tokenize about 4 ms.","files":null}]}