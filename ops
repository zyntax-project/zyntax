{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1789823794,"nonce":"Q2FbSa73kSrMbkkr78qaR0gxIR8=","title":"Hot reload: an interpreted caller of an edited fiber def calls a function the module does not hold","message":"`zynml/tests/hot_reload_effect_fibers.rs::a_handler_edit_retargets_dispatch_at_the_next_perform`\nfails at e7327eb and before this session's changes: after\n`reload_typed_program` edits the handler, `first_step` (interpreted) calls\n`probe()` and the interpreter reports \"undefined SSA value HirId(N)\",\nwith `[interp] call ([])`: the callee id is the edited program's fiber\nbody, which is not in the interpreter's module (it holds machine,\nFeed$next_event, drive, probe, first_step). The remap of the edited\nmodule's ids onto the running program's misses that function, or the\ninterpreter's cached body of `first_step` was rebuilt against the new\nids.\n\nReproduce: `cargo test --release -p zynml --test hot_reload_effect_fibers\n-- a_handler_edit_retargets`, deterministic. The baseline test with the\nsame program and no reload passes.","files":null}]}