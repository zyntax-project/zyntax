{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1789496196,"nonce":"nTzOQuQBMYeqg57mnAhtRhkeZng=","title":"float and nbody_modified die with a signal on zypy","message":"Both speed-center kernels compile and start, then the process dies with a signal before the first iteration's time is printed (`zypy run float.py -n 1` exits 139; nbody_modified likewise). The shapes they share and the rest of the suite does not: nested tuple and list patterns as `for` targets (`for (([x1, y1, z1], v1, m1), ([x2, y2, z2], v2, m2)) in pairs:`), `dict.values()` iterated, default arguments naming module globals (`def advance(dt, n, bodies=SYSTEM, pairs=PAIRS)`), `** -1.5`, `//` on floats, and float.py's `Point` class with `maximize`/`normalize` methods called through a list of instances. Bisect by cutting the kernel down until the signal goes; a signal, not a diagnostic, means a miscompile or a bad free. Speed-center row: harness 39c723b, gap list 89aaf85.","files":null}]}