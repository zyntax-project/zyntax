{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1789920788,"nonce":"23Lh4kmHO10q8KqMGsQlHJXeRck=","title":"Polymorphic instance values box: a list of (Sphere|Halfspace, SimpleSurface|CheckerboardSurface) is List\u003cObject\u003e","message":"raytrace keeps scene.objects as a list of (shape, surface) pairs where the shapes and surfaces are of two classes each. join(Class(a), Class(b)) is Object, so every element is boxed, every method call on it (intersectionTime, colourAt, normalAt) goes through the py$call$m$N dispatcher, and every result is a box. The same shape appears in go (nodes of several classes), deltablue (constraint subclasses) and richards (task subclasses via a base). A class-set type (an instance of one of a known set of classes, held as the pointer it is, dispatched by class tag) would keep these unboxed: join of two classes with a common base becomes the base, or a set; method calls on the set switch on the tag over the members only; fields common to all members are read directly. Measure raytrace and go before and after.","files":null}]}