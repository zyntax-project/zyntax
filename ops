{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1789929634,"nonce":"mVRcozpReJVq5vYe92X3HaPjLuY=","title":"interpreter: a store into a bool field drops a value that is not a Bool","message":"`write_typed` for `HirType::Bool` writes only a `ZyntaxValue::Bool`. A\ntruth value the interpreter computed as an integer (the result of a\ncompare or a bitwise operation on i64, or a `not` lowered as `xor 1`)\nis silently not stored, so a struct's bool field keeps its previous\ncontents.\n\nReproducer: the Lua slots work stores `not self.state` into a shaped\ntable's Bool slot; on the interpreter tier `self.state` never changed,\non Cranelift and LLVM it toggled.\n\nFix: accept any value `value_to_i64` reads and store `n != 0`.","files":null}]}