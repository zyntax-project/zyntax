{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1789496196,"nonce":"gEa3Qz8qR+QmgIkkavXlb3GnbSc=","title":"The random module, with CPython's generator","message":"chaos and go seed `random` and then draw from it (`random.random`, `randrange`, `randint`, `choice`, `shuffle`, `seed`); go plays games whose moves come from the draws, so a different generator is a different amount of work and no comparison. The generator must be CPython's: Mersenne Twister MT19937 seeded as `random.seed(int)` seeds it (init_by_array over the integer's 32-bit words), `random()` from two draws as CPython's `random_random`, `getrandbits`, and `randrange`/`randint`/`choice`/`shuffle` built on `_randbelow_with_getrandbits` exactly as Lib/random.py builds them, so the sequences match digit for digit. Verify against CPython with a fixed seed. Gap list 89aaf85.","files":null}]}