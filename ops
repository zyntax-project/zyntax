{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1790274427,"nonce":"t7rEMIjs4LVOqfNtqy+W4eG4w6Q=","title":"zylua mandelbrot_strip.lua printed a wrong checksum once under load","message":"`ZYLUA_LLVM=1 zylua run crates/zyntax_lua/benchmarks/mandelbrot_strip.lua` printed `result: 228015` once (lua5.4 prints `result: 908586`). Seen on the Mac with a debug build carrying `--features llvm-backend` (branch w2/R-string-header-immortals, main 8844ca79 plus the string-header change, which does not touch numeric code), while a second debug zypy run loaded the machine.\n\nNot reproduced since: 0 of 20 further runs of that debug binary, 0 of 30 runs each of the release binaries built from main 8844ca79 and from the branch. The kernel is numeric only (no strings besides the final `string.format`), so this looks like a tier-transition race (OSR entry or the LLVM install racing the running loop) rather than anything in the value computation; a checksum from a partly-run strip would be smaller, as this one is.\n\nTo chase: loop the debug binary with `ZYNTAX_OSR_TRACE=1` under a parallel CPU load until it recurs, and compare `ZYNTAX_DISABLE_OSR=1`.","files":null}]}