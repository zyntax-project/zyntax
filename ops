{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":3,"timestamp":1790316006,"nonce":"50Alsm43mE++6UUT3gBHCO8rKck=","message":"Also seen through the Python frontend (git-bug 382b32e): unbox_tuple in a lowerer without guards (the py$getattr$\u003cfield\u003e dispatchers) emitted Block[Let held = zb_unbox_tuple_len(..); Let t = zero; If(held != 0) { t = tuple } else { t = zero }; t]. The pre-opt HIR of py$getattr$pos shows both branches building their struct and the merge block reading the Let's zero (no phi), so every tuple field read through a dynamic receiver was (0, 0). Confirms it is the SSA builder: process_statement's on-demand If arm gives the continuation block the pre-if definitions and never merges what the branches wrote. The Python side now emits an If expression there instead.","files":null}]}