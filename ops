{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1789391249,"nonce":"sh677DzW4dhqFRvK6Yxe7YXnV18=","title":"Python: a field settled by a test stayed settled across a store or a call","message":"`nonnull_fields` (8f53003) recorded that `self.left` holds an instance\nafter `if self.left is not None:` but nothing forgot it: a store to the\nfield, or a call to a method that stores to it, left the fact standing\nand the following `self.left.item` read through null.\n\n    if self.left is not None:\n        self.left = None\n        return self.left.item     # segfault; CPython raises AttributeError\n\nNow a store to a field of that name drops every fact about it, on any\nobject, and any call drops them all. Conformance\n`class/field_settled_then_changed.py`.\n\nFound alongside: class field layout followed a HashMap's iteration\norder, so `Node` was `{tag, left, item, right}` in one run and\n`{tag, item, left, right}` in the next, and binary_trees timed\ndifferently run to run. Fields are laid out in the order `__init__`\nfirst writes them.","files":null}]}