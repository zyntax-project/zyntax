{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1790445848,"nonce":"tRP4Oe52IHwMnm4amWdgesPu1Tc=","title":"Inference: lists filled through a subscript of another list stay dynamic","message":"`fps = [[] for c in range(n)]` followed by `fps[c].append(x)` (and the\ntwo-level form meteor-contest uses, `fps[a][b].append(fp)` into\n`[[[] ...] ...]`) types fps as List(Object): list_sites follows appends\non a name, not on an element of it, and an empty list literal inside a\ncomprehension is Unknown. meteor's solve therefore gets fps and every\nfootprint as Object, so `for ci in fp` and the footprint sets' algebra\ngo through the dynamic layer; the brief's\n`fps: List(List(List(Set(Int,frozen))))` needs this. solve's `free` is\ntyped Set(Int,frozen) already.","files":null}]}