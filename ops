{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1789513152,"nonce":"mIJYKafqQyT8JhRY5UslfP5WOjU=","title":"Python: a call of a builtin as a value other than range crashes","message":"`f = len; f([1, 2])` dies with a signal: a bare builtin name used as a value fell through to a variable read of a name nothing defines. Only `range` has a record (a variadic adapter). Now every other builtin used as a value is refused with \"`len` as a value is not supported yet\", and a name bound once to a builtin is typed `Builtin(k)` so calls through it lower as the builtin's own call (`xrange = range` was the speed-center kernels' shape). Records for the rest of the builtins are still owed, so a builtin can be passed to `map`, stored, or returned.","files":null}]}