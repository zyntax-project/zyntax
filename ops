{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1789253667,"nonce":"A1JJRyQJCsuwwa/CKt1OnSh9S9E=","title":"Python: iteration builtins, key= and reverse=","message":"Missing builtins that most scripts reach: `enumerate`, `zip`, `map`,\n`filter`, `any`, `all`, `reversed` on anything but a list, `iter` and\n`next` on a list, `sorted`/`min`/`max`/`list.sort` with `key=` and\n`reverse=`, `sum` with a start value, `round` with digits, `divmod`\non floats, `pow` with three arguments.\n\nEach is a `zb_*` function over the list and Any primitives plus a\ntyper entry. `key=` takes a function value (a FUNC_TAG record called\nthrough `zb_call_1`) or a builtin as a value, which is gap 2 of\nf1b2928; `enumerate`/`zip` produce tuples, so the tuple box exists\nalready. `map`/`filter`/`zip`/`enumerate` are lazy in CPython; an\neager list is observably equivalent unless the argument is infinite\nor has side effects, and the generator form can follow 4f32f29.","files":null}]}