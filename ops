{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":3,"timestamp":1789229921,"nonce":"CPqDuRkPtb0OYDVVv3M2nJQUs8U=","message":"Print half fixed: Type::Any now lowers to Ptr(Opaque(\"DynamicBox\")) in both type converters, emit_box_to_any produces that type (and boxes strings via a new zyntax_box_str), and each backend's signature-driven boxing passes a value of that type through untouched. show(5)/show(\"text\")/show(2.5)/show(true) print their values.\n\nReturn half still open: `def pick(flag: bool): Any { if flag { 7 } else { \"fallback\" } }` returns null. The tail if-expression carries no declared type (the parser leaves it Unknown), so the new meet_declared_any hook in the If lowering has nothing to meet, and the Return arm in process_terminator hands the merge value back without coercing it to the function's declared return type. Fix: the Return arm coerces to the declared return type the way a call argument does (coerce_for_transfer), which boxes the i64/String merge.","files":null}]}