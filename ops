{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":3,"timestamp":1789230599,"nonce":"9y3DTu+cpmvC53IoYSFEunpGGps=","message":"Return half: coerce_for_transfer at the return now runs in the block where evaluation ended (it was emitted into the block the return started in, ahead of the branch). `def seven(): Any { return 7 }` and `def text(): Any { return \"fallback\" }` print 7 and fallback, and `let n: i64 = a` unboxes.\n\nWhat remains is ZynML-specific: a block-bodied `if` used as the tail value of a function (`def pick(flag: bool): Any { if flag { 7 } else { \"fallback\" } }`) lowers to a CondBranch whose arms produce no value and returns an undef; the arms' values are discarded. That is the statement-as-value path, not the If expression, and is tracked here as the last item.","files":null}]}