{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1790445831,"nonce":"e+VcscNrr1r7zdDHt9IfuuWP4rc=","title":"Typed dicts: what P-typed-dicts left of its brief","message":"Landed on main through 3378b3a1: Ty::Set(u16) with frozenness, per-store\ndict and set tables, cross-kind probes through the stores' *_any\nfunctions, direct iteration with the size-change RuntimeError,\nElem::Dict/Elem::Set and dict/set fields by address, and shape classes.\nNot done:\n\n- Literal Str keys do not fold mix(fnv(key)) at compile time: d[\"k\"]\n  hashes the literal at run time. Wants a per-shape find_h/get_h/set_h\n  taking the hash, the Rust replica (zrtl::fnv1a_bytes then the\n  zb_hash_mix steps, arithmetic shifts), and a unit test against the\n  runtime.\n- Instance keys: a key of class type is stored as a dynamic value. The\n  brief's rule (identity hash when no class in the key class's\n  hierarchy defines __eq__ or __hash__) is not implemented.\n- join(None, Dict|Set) still makes the value dynamic (no nullable\n  container machinery landed).\n- A tuple probe of another shape, and a float or other-kind probe, box\n  the key and go through find_any; there are no per-(stored, probe)\n  dict_probe_by instances.\n- dict(list_of_pairs) builds a library dict and converts it into the\n  typed store (two copies); a typed list of 2-tuples could be stored\n  directly. dict(genexp) already lowers as a comprehension.","files":null}]}