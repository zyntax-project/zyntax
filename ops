{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1790317646,"nonce":"FNWOoPpK+80v3gNdi1+P/LrVZYQ=","title":"crypto_pyaes segfaults after about 13 iterations with the LLVM tier on","message":"`zypy run crypto_pyaes.py -n 20` (speed kernel staged as run.py stages it) dies with SIGSEGV on main 4771a187, release --features llvm-backend, ZYPY_LLVM=1, on both the Mac (arm64) and the NUC (x86_64). -n 5, 8 and 12 finish; -n 16 and above crash every time. The speed center's own trial count (50, run.py without --fast) therefore cannot run this kernel.\n\nNot the quick baseline: ZYNTAX_DISABLE_QUICK_BASELINE=1 crashes the same way. ZYNTAX_DISABLE_OUTLINE=1, ZYNTAX_DISABLE_OSR=1 and ZYNTAX_DISABLE_WARM_UP=1 each make it finish; ZYNTAX_DISABLE_STATIC_HOT=1 does not. Without ZYPY_LLVM it finishes.\n\nlldb: EXC_BAD_ACCESS (address 0x1c) in JIT code, called from HirInterpreter::call_native_through \u003c- call_function_natively \u003c- call_by_id \u003c- run_frame (main's interpreted loop calling benchmark natively). The faulting frames do not map to any [ptrs] publication, so they are most likely an outlined region or its LLVM promotion (the slow runs of the same kernel promote pyaes$CBCMode$decrypt$resume1, an outlined region with \"RegionHasExternalEntry\" rejects in the OSR trace).","files":null}]}