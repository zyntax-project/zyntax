{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1789496211,"nonce":"9NGHRBaqjpxzBZ58I+g/RDcVitU=","title":"Modules the remaining speed-center kernels import: re, string, codecs, json, os, hashlib, io, decimal, struct","message":"ai imports `re` and `string`; crypto_pyaes `codecs` (and bytes); json_bench `json`; pyflate-fast `os`, `hashlib`, file IO and bytes; hexiom2 `io.StringIO`; telco `decimal`, `struct` and file IO. Each is a stdlib module the frontend has no table for, and most carry a runtime (regular expressions, decimal arithmetic, hashing) rather than a few functions. One issue per module when one is taken up; this one holds the list. Gap list 89aaf85.","files":null}]}