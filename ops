{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1790428713,"nonce":"WxGFDtky4zGQf6ZnG9sxeL1HaKk=","title":"An if-expression of the builtins DSL over a list element load, nested in another, lowers to an Undef index","message":"Seen while writing the dict library: with Table::ctrl written as\n\n    if_expr(eq(len(d), 0), int(0), idx(idx(d, int(0), entry_ty), int(0), i64()))\n\nand Table::first as if_expr(ne(ctrl(d), 0), int(1), int(0)), used as an index (add(p, first(ctrl(d)))), the Python conformance case set/basics.py stops with 'type mismatch: expected integer (GEP index), got Undef' in the HIR interpreter, and several dict/set/json cases fail the same way. Making ctrl a function (zb_dict_ctrl: early return when empty, then the load) fixed every case, so the nested conditional value is what loses its value. Not isolated to a HIR test yet.","files":null}]}