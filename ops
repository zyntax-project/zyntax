{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":3,"timestamp":1790312818,"nonce":"UfdxRLhwLg8MreOaH2ycyjypGv8=","message":"Integration of w2/L-gc-semantics (86b9e320) onto perf-integration ae08e974 was not merged: two of the package's own gc cases fail in a debug build on the Mac (arm64), in all three harness modes (plain, warm-up on, collector stressed), with ZYLUA_LLVM=1 and without.\n\ngc/finalizers.lua: the \"phoenix\" object is not finalized by the first collectgarbage(), so line 2 prints nil instead of phoenix (5 of 5 runs).\ngc/weak_value_keys.lua: one key survives: \"after 2 1 5\" / \"after 3 1 1\" instead of 0 5 / 0 0; key 3 is finalized only at exit.\n\nThe package tip built alone (debug, llvm-backend, in its worktree) fails the same two cases the same way, so this is not a merge interaction. Release binaries (merged tree) pass all 13 gc cases, tiered and ZYLUA_LLVM=1, with and without ZYNTAX_GC_FLOOR_KB=1. The difference is the host's debug frames: a stale word left in a host frame keeps the object reachable. Scrubbing 32 KB below host_gc before collector::collect did not change it, so the word sits in host_gc's own frame or in a frame above it. Either the cases need to be robust to debug host frames, or the host path (setmetatable's note, host_gc) must not leave the object's address where the collection's frames read it.\n\nThe merged state (conflicts resolved: one doc comment on shape_layouts in lower.rs) is kept at refs/backup/integration/L-gc-semantics-merged (14f9e1f8). Everything else on it passed: fmt, clippy (compiler + lua, llvm-backend), Lua conformance otherwise 34/37 with only the gc tests failing, memory_pressure 1/1, Python conformance 50/50.","files":null}]}