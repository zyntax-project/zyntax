{"author":{"id":"b409816c5cd5ab140d1d52eb33aace35de44952bb794b19806fd57f6ecb1ace9"},"ops":[{"type":1,"timestamp":1790362076,"nonce":"nfVzPjzt42sLspiKSWgfRaoxs+k=","title":"Book chapter 13 describes the legacy async lowering and its examples do not compile","message":"book/13-async-runtime.md documents the legacy path (`__foo_poll` names, a u32 state with captures at offset 4) and never mentions the krio lowering the runtime takes, `sleep` and host bridges, `await_raw`, `drive_tasks` or wasm. Its examples do not hold against the code:\n- match examples omit `PromiseState::Cancelled` and `assert_eq!` a `PromiseState`, which does not implement the comparison;\n- the `then`/`catch` chaining example returns an error;\n- `poll_with_limit` is described as polling repeatedly; it fails once the count is reached;\n- a result `\u003c 0` is described as Ready; the host reports it as Failed;\n- `get_function_tier` and `is_completed` do not exist;\n- `await_result` is described as blocking until done; it never drives timers.\n\nRewrite the chapter against docs/ASYNC_EFFECTS_FIBERS.md sections 6 and 8, with examples compiled by a doc test.","files":null}]}