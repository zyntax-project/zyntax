[package]
name = "async_ir_pass"
version = "0.1.0"
edition = "2021"
description = "Async-IR pass — transforms async/await into state machines at TypedAST level"

[dependencies]
pattern_engine = { path = "../../pattern_engine" }
normalization_pass = { path = "../normalization" }
zyntax_typed_ast = { path = "../../typed_ast" }
log = "0.4"
