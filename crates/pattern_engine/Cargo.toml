[package]
name = "pattern_engine"
version = "0.1.0"
edition = "2021"
authors = ["Damilare Akinlaja"]
description = "Term-rewriting pattern engine for TypedAST transformations"

[dependencies]
zyntax_typed_ast = { path = "../typed_ast" }
log = "0.4"
indexmap = "2"

[dev-dependencies]
env_logger = "0.11"
