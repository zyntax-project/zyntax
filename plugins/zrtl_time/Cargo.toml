[package]
name = "zrtl_time"
version = "0.1.0"
edition = "2021"
description = "ZRTL Time plugin - time, duration, sleep operations"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
zrtl = { path = "../../sdk/zrtl" }

[lints]
workspace = true
