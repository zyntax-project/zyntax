[package]
name = "zrtl_process"
version = "0.1.0"
edition = "2021"
description = "ZRTL Process plugin - subprocess execution and management"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
zrtl = { path = "../../sdk/zrtl" }

[lints]
workspace = true
