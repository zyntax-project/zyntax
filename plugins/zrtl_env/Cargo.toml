[package]
name = "zrtl_env"
version = "0.1.0"
edition = "2021"
description = "ZRTL Environment plugin - env vars, args, process"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
zrtl = { path = "../../sdk/zrtl" }

[lints]
workspace = true
