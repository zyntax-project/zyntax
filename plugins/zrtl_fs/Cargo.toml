[package]
name = "zrtl_fs"
version = "0.1.0"
edition = "2021"
description = "ZRTL File System plugin - file and directory operations"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
zrtl = { path = "../../sdk/zrtl" }

[lints]
workspace = true
