[package]
name = "zrtl_io"
version = "0.1.0"
edition = "2021"
description = "ZRTL I/O plugin - print, input, formatting"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
zrtl = { path = "../../sdk/zrtl" }

[lints]
workspace = true
