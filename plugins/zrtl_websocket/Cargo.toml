[package]
name = "zrtl_websocket"
version = "0.1.0"
edition = "2021"
description = "ZRTL WebSocket plugin - WebSocket client for real-time communication"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
zrtl = { path = "../../sdk/zrtl" }
tungstenite = "0.24"
lazy_static = "1.5"

[lints]
workspace = true
